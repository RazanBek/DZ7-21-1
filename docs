1) Это структуры которые могут хранить данные вместе.
2) Они нужны для хранения данных.
3) Это структура данных, которая хранит данные в виде пары ключ-значение т.е словарь в пайионе.
4) Это структура данных.Связный список обеспечивает возможность создать двунаправленную очередь из каких-либо элементов.
Каждый элемент такого списка считается узлом. По факту в узле есть его значение,
а также две ссылки – на предыдущий и на последующий узлы. Также есть и другой вид LinkedList'а, который хранит тоько
одну ссылку на последующий элемент списка.
5) LinkedList - Используется когда не хватает ОЗУ, т.е в очень больших проектах.
   А Hash Map - Когда нужно хранить данные в виде ключ-значение т.е это просто словарь